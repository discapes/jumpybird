{
  "name": "jumpybird",
  "version": "1.0.0",
  "description": "Classic arcade game.",
  "scripts": {
    "start": "serve public",
    "babel": "cross-env BABEL_ENV=production babel src -o public/build/bundle.js",
    "cleancss": "cleancss -o public/build/styles.min.css src/*.css",
    "build": "concurrently npm:babel npm:cleancss",
    "watch-babel": "cross-env BABEL_ENV=production babel src -o public/build/bundle.js --watch",
    "watch-cleancss": "cleancss -o public/build/styles.min.css src/*.css --watch",
    "watch": "concurrently npm:watch-*"
  },
  "author": "Miika Tuominen",
  "devDependencies": {
    "@babel/cli": "^7.18.6",
    "@babel/core": "^7.18.6",
    "babel-preset-minify": "^0.5.2",
    "clean-css-cli": "^5.6.1",
    "concurrently": "^7.2.2",
    "cross-env": "^7.0.3",
    "serve": ">=13.0.4"
  }
}
